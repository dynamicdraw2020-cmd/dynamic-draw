Dynamic D 1.0.1 검증 결과

검증 기준:
- npm install --no-audit --no-fund
- npm run typecheck
- npm run lint
- NEXT_TELEMETRY_DISABLED=1 npm run build

결과:
- TypeScript 검사 통과
- ESLint 검사 통과
- Next.js 프로덕션 빌드 통과
- 신규 라우트 확인:
  /dashboard
  /event-live
  /api/admin/products
  /api/admin/products/[id]
  /api/admin/draws/[id]/attach-product
  /api/admin/currencies/[id]
  /api/admin/ticket-exchange-rates/[id]
- 빌드 경고:
  components/user-roulette-draw.tsx의 외부 이미지 URL 호환용 <img> 경고 2개만 존재합니다.
  관리자 입력 이미지 URL을 그대로 허용하기 위한 의도된 경고입니다.

DB SQL은 idempotent 형태로 작성되어 반복 실행 시 핵심 테이블/컬럼 생성은 안전하게 넘어갑니다.
단, 기존 뽑기 화면 제거 처리는 site_settings의 v1_0_1_operation_redesign_applied 마커가 없을 때 한 번만 실행됩니다.
