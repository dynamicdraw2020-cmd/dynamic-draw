Dynamic D LAST FIX validation report

Scope:
- Full project ZIP provided by user: dynamic-draw-clean.zip
- Files changed:
  - app/api/auth/login/route.ts
  - app/api/auth/reset-password/route.ts
  - components/auth-form.tsx
  - RUN_ONCE_FINAL_RECOVERY_SQL.sql

Fixes included:
1. Admin recovery login no longer depends on Supabase Auth password verification.
   - dynamicdraw2020@gmil.com + DynamicD2026!reset always creates emergency session and redirects to /reset-password.
   - dynamicdraw2020@gmil.com + DynamicD2026!final creates emergency session and redirects to /admin.
2. Password reset stores the new password in dynamicd_auth_credentials first.
3. If the credential table schema is old, reset-password falls back to older insert shape and then RPC.
4. SQL creates/repairs:
   - profiles.must_change_password
   - profiles.password_reset_at
   - profiles.password_changed_at
   - dynamicd_auth_credentials required columns
   - dynamicd_check_login_password
   - dynamicd_set_login_password
   - dynamicd_set_password
   - admin auth/users/identities/profile coherence
5. Signup duplicate passwordConfirm input removed.

Validation performed locally:
- npm run typecheck: PASS
- npm run lint: PASS with warnings only, 0 errors
- npm run build: PASS

Important runtime test after deploy:
1. Run RUN_ONCE_FINAL_RECOVERY_SQL.sql in Supabase SQL Editor.
2. Confirm the result row contains reset_password_ok = true.
3. Deploy this ZIP.
4. Login with:
   ID: dynamicdraw2020@gmil.com
   Password: DynamicD2026!reset
5. You should be redirected to /reset-password.
6. Save a new password of 8+ characters.
7. You should be redirected to /account.
8. Logout and login again with the new password.

If testing direct admin fallback instead of reset:
- Login with dynamicdraw2020@gmil.com / DynamicD2026!final
- This routes directly to /admin.
